using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class boasPraticas : MonoBehaviour
{
    /*1-Nao mudar o nome da classe sem mudar o nome do arquivo e vice versa. Isto acusara erro.
        A linguagem c# eh case sensitive, ou seja, eh sensivel a letras miniculas e maiusculas. ou seja, Teste eh diferente de teste!
    2-  Todas as funcoes herdadas usam o PascalCase, ou seja, todos os comandos nativos da Unity, usa-se o PascalCase.
        Todas as nossas propriedades e funcoes, usamos camelCase. 
  
    */

    //modificador     tipo     nome;
    public string nomeAluno;
    public int idadeAluno;
    public bool isAprovado;




    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
